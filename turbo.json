{
  "$schema": "https://turborepo.dev/schema.json",
	"globalEnv": ["DATABASE_URL", "GEO_DATA_DIR", "GEO_WORKER_HOST", "GEO_WORKER_PORT", "DEEPSEEK_API_KEY", "DEEPSEEK_MODEL", "DEEPSEEK_BASE_URL"],
  "tasks": {
    "//#format": {
      "cache": false
    },
    "//#lint": {},
    "//#lint:fix": {
      "cache": false
    },
    "build": {
      "dependsOn": ["^build", "check-types"],
      "outputs": [".output/**", ".vercel/output/**", "dist/**"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "test": {
      "dependsOn": ["^build"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}
